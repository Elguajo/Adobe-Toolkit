{
  "schema": 1,
  "default": {
    "required": [],
    "skills": [],
    "notes": []
  },
  "phases": {
    ".progressive/phases/00-native-macos-gui-specification.md": {
      "required": [
        "macos/AdobeBackuper.command",
        "macos/clean/lib/adobe-cleaner-macos.sh",
        "run-macos.command"
      ],
      "skills": [
        "architecture-decision",
        "security-sensitive-change"
      ],
      "notes": [
        "Read targeted backend contracts only; do not load all runtime integrations or completed history.",
        "Read gui/adobe-toolkit/macos/INTERACTION_SPEC.md and shared/contracts/macos-gui-adapter-v1.md only when changing the GUI or its adapter.",
        "The planned macOS GUI location is gui/adobe-toolkit/macos/; do not modify current CLI terminal workflows.",
        "Do not implement macOS GUI privileged operations without future explicit user authorization."
      ]
    },
    ".progressive/phases/01-native-windows-gui-parity.md": {
      "required": [
        "run-windows.cmd",
        "windows/adobe-backup.ps1",
        "windows/clean/AdobeCleaner.ps1",
        "shared/cleaner-manifest.json"
      ],
      "skills": [
        "security-sensitive-change"
      ],
      "notes": [
        "The planned Windows GUI location is gui/adobe-toolkit/windows/; preserve every existing cmd/PowerShell workflow.",
        "The current Windows scripts have no structured GUI contract. Never parse their human-readable stdout or use run-windows.cmd as a GUI fallback.",
        "AdobeCleaner.ps1 -Mode DryRunFull currently stops processes/services and writes a log; it is not a GUI-safe cleanup preview.",
        "Do not implement Windows GUI privilege elevation, Full Cleanup, or Repair without future explicit user authorization."
      ]
    }
  }
}
